# 告警-风险-异常汇总表

针对 [[PCM/PCM/index|PCM]] 已知各类问题的应急处置方案及运维参考链接：

*   [《PCM应急处置》](https://alidocs.dingtalk.com/i/nodes/MNDoBb60VLYDGNPytBomBqkPJlemrZQ3)
*   [《PCM运维手册》](https://alidocs.dingtalk.com/i/nodes/amweZ92PV6DbOdgzUK4on0qD8xEKBD6p)
*   [《PCM排查思路&常见问题》](https://alidocs.dingtalk.com/i/nodes/m9bN7RYPWdyrPBREckdQ5joEVZd1wyK0)

## 异常识别与处置总则

当遇到访问报错，怀疑是 PCM 禁用 AK 导致的，优先通过拦截日志判定。提取日志中的请求 AK，并通过 PCM 服务查询 AK 状态；如果确认已经禁用，应立即采用应急处置方案进行止血，并反馈研发侧排查根本原因。

**应急处置优先级原则**：
应急操作优先建议控制台白屏操作，当白屏无法访问时，采用在容器中执行脚本（调用服务接口），当容器无法访问时，直接在数据库中执行 SQL。
**控制台白屏 > 调用接口（容器脚本） > 数据库执行 SQL**

## 告警/风险/异常汇总表

以下汇总表按优先级（P1 立即止血 -> P4 忽略/暂缓）排列，帮助值班人员快速识别并处置各类异常与风险。

| 告警名 | 级别 | 识别方式 | 所属组件 | 触发条件 | 含义 | 应急手册链接 | 是否有EOCC/KB |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **AK 调用网关被拦截** | P1 (立即止血) | 网关日志报 AK 被禁用/无效/不存在；产品调用网关报错 | PCM Core / 业务网关 | 底表AK被禁用且SDK降级失败，或派生AK被轮转禁用且未及时更新 | 凭据失效，业务请求被网关拦截，业务中断 | [《PCM应急处置》](https://alidocs.dingtalk.com/i/nodes/MNDoBb60VLYDGNPytBomBqkPJlemrZQ3) / [《PCM运维手册》](https://alidocs.dingtalk.com/i/nodes/amweZ92PV6DbOdgzUK4on0qD8xEKBD6p) | 否 |
| **OSS 访问拦截** | P1 (立即止血) | 日志包含特征：`"error_code": "InvalidAccessKeyId"` 且 `"status": "403"` | OSS | PCM禁用AK导致访问OSS报错 | AK被禁用，OSS拒绝访问 | 提取请求AK查询状态，若已禁用则采用 [《PCM应急处置》](https://alidocs.dingtalk.com/i/nodes/MNDoBb60VLYDGNPytBomBqkPJlemrZQ3) 并反馈研发排查 | 否 |
| **SLS_INNER 访问拦截** | P1 (立即止血) | 日志包含特征：`"Status": "401"` | SLS_INNER | PCM禁用AK导致访问SLS内部服务报错 | AK被禁用，SLS内部服务拒绝访问 | 提取请求AK查询状态，若已禁用则采用 [《PCM应急处置》](https://alidocs.dingtalk.com/i/nodes/MNDoBb60VLYDGNPytBomBqkPJlemrZQ3) 并反馈研发排查 | 否 |
| **SLSPUB 访问拦截** | P1 (立即止血) | 日志包含特征：`"Status": "401"`，`"ErrorCode": "Unauthorized"`，`"ErrorMsg": "AccessKeyId is disabled: [AK]"` | SLSPUB | PCM禁用AK导致访问SLS公网服务报错 | AK被禁用，SLS公网服务拒绝访问 | 提取请求AK查询状态，若已禁用则采用 [《PCM应急处置》](https://alidocs.dingtalk.com/i/nodes/MNDoBb60VLYDGNPytBomBqkPJlemrZQ3) 并反馈研发排查 | 否 |
| **ASAPI 访问拦截** | P1 (立即止血) | 日志包含特征：`"errorCode": "asapi.server.request.parameter.accesskeyid.error"`，`"errorMessage": "The specified AccessKey ID ([AK]) is invalid. Details: (The Access Key is disabled.)."` | ASAPI | PCM禁用AK导致访问ASAPI报错 | AK被禁用，ASAPI拒绝访问 | 提取请求AK查询状态，若已禁用则采用 [《PCM应急处置》](https://alidocs.dingtalk.com/i/nodes/MNDoBb60VLYDGNPytBomBqkPJlemrZQ3) 并反馈研发排查 | 否 |
| **KMS 访问拦截** | P1 (立即止血) | 日志包含特征：`"error_code": "Forbidden.AccessKey"`，`"error_message": "This AccessKey is not enabled."`，`"status_code": "403"` | KMS | PCM禁用AK导致访问KMS报错 | AK被禁用，KMS拒绝访问 | 提取请求AK查询状态，若已禁用则采用 [《PCM应急处置》](https://alidocs.dingtalk.com/i/nodes/MNDoBb60VLYDGNPytBomBqkPJlemrZQ3) 并反馈研发排查 | 否 |
| **UMMAK 单 UID AK 容量超限** | P1 (立即止血) | 派生失败报错 / 数据库查询超限UID | UMMAK / PCM | 单个UID下有效AK数量达到1000把 | UMMAK侧每个uid下最大1000把有效AK，达到上限后会导致新的AK派生失败 | [容量问题数据处理](https://alidocs.dingtalk.com/i/nodes/QG53mjyd800agdlKHbek2aXQ86zbX04v) / 见下文【容量告警场景】 | 否 |
| **UMM 账户 AK 上限打满 (多集群配置)** | P1 (立即止血) | UMM 账户 AK 数量监控/创建派生 AK 失败日志 | UMM / PCM Controller | 派生 AK 队列级别配置为 ClusterName 级别，且存在多集群 | 多集群为同一个底表 AK 创建多个独立队列，叠加后把 UMM 账户的 AK 上限打满，导致无法创建新的派生 AK | 修改队列级别配置为 initAK 级别（默认推荐） | 否 |
| **initAK被禁用异常** | P1 (立即止血) | 业务报错 / 白屏查询AK状态 | PCM / UMMAK | 某把initAK被禁用 | initAK被禁用导致依赖该AK的业务请求失败 | 见下文【启用某个已经禁用的initAK】 | 否 |
| **底表AK被禁用异常** | P1 (立即止血) | 业务报错 / 数据库查询禁用状态 | PCM / UMMAK | 环境内存在被禁用的底表AK | 底表AK被禁用导致相关凭证派生或业务受影响 | 见下文【启用全量底表AK】 | 否 |
| **派生AK被禁用异常** | P1 (立即止血) | 业务报错 / 白屏查询AK状态 | PCM / UMMAK | 某把派生AK被禁用 | 派生AK被禁用导致使用该AK的业务调用失败 | 见下文【启用派生AK】 | 否 |
| **Java 应用线程阻塞** | P1 (立即止血) | 线程 dump 出现 `BLOCKED` 状态，堆栈含 `NativePRNG$RandomIO` 和 `PcmSecretCredentialManager` | Java SDK | 系统熵值低（< 100），SDK 默认使用 `/dev/random` 阻塞模式获取随机数 | 应用线程卡死，影响业务处理 | 升级 SDK 至 `credprovider.plugin >= 1.0.8` 或添加 JVM 参数 `-Djava.security.egd=file:/dev/./urandom` | 否 |
| **PCM Controller 磁盘打满 / 产生大量日志** | P1 (立即止血) | 磁盘空间不足告警；`df -h` 确认；日志目录 `/home/admin/pcm_controller/logs/api/logs/` 出现超大文件 | PCM Controller | 大量异常请求持续打到 Controller 或定时任务异常导致循环报错，且日志轮转配置异常 | 磁盘打满导致 Controller 服务不可用 | 清理历史日志，排查异常请求/任务，确认日志轮转配置 | 是 ([EOCC参考](https://eocc.aliyun-inc.com/kbscene/emergencyDetail/EC9EE9AE20?Jump=2)) |
| **Core 返回 502 (限流触发/IP误伤)** | P1 (立即止血) | SDK/客户端收到 HTTP 502 错误 | PCM Core | QPS 超过限流阈值（单核 x86=200r/s等），或同 IP 下其他高频产品耗尽配额导致误伤 | 请求被限流拒绝，可能导致凭据获取失败 | 检查 `access.log` 中 `limit_req_status`，调整 `pcm_core.json` 限流配置 | 否 |
| **Core 认证/参数失败 (HTTP 403/400)** | P1 (立即止血) | Core 返回 403（signature error/nbf not valid）或 400（参数为空/格式错误/AK非底表） | PCM Core / SDK | 签名计算不一致（regionid/SK/endpoint错误）、时钟不同步、SDK初始化参数配置错误 | 认证或参数校验失败，无法获取派生凭据 | 核对 SDK 初始化参数、检查 NTP 同步、确认 SK 已解密；参考 Core 错误码快速定位 | 否 |
| **CLI 工具报错 ResponseParseFailure** | P1 (立即止血) | CLI 返回 `{"code": "ResponseParseFailure", ...}` | CLI 工具 | `pcm_endpoint` 地址错误，响应 200 但格式非预期，且旧版本未走降级 | CLI 直接不可用 | 确认 `pcm_endpoint` 指向正确地址；升级至 2025-12-23 及之后版本 | 否 |
| **半轮转模式首次获取失败导致后续持续异常** | P1 (立即止血) | 产品持续使用底表 AK 或无有效凭据运行，SDK 无刷新日志 | 业务应用 / SDK | 采用半自动轮转模式，启动时唯一一次获取请求失败（限流/网络抖动/服务未就绪） | 凭据无法自动恢复，业务持续异常 | 重启服务重新触发获取，或改为全自动轮转模式 | 否 |
| **底表禁用后 PCM 不可用导致业务中断** | P1 (立即止血) | 服务重启后无法获取凭据，业务直接中断 | 业务应用 / PCM 链路 | 底表 AK 被禁用，且服务重启时 PCM 链路不可用，本地无缓存 | 凭据供给完全依赖 PCM 链路，链路故障导致无凭据可用 | 恢复 PCM 链路，或临时在控制台启用底表 AK | 否 |
| **PCM 与应用均异常且 SDK 缓存丢失** | P1 (立即止血) | 应用启动失败/凭证获取失败告警 | PCM SDK / 云产品应用 | PCM 和应用都挂了需重拉，且 SDK 本地缓存丢失 | SDK 无法获取凭证且无本地缓存降级，导致业务中断 | 需先恢复 PCM 或使用老凭证应急脚本 | 否 |
| **Go SDK 日志文件持续增长 / 不轮转** | P2 (计划处理) | Go SDK 日志文件不断增大，未按预期轮转，磁盘空间告警 | Go SDK | 使用 2512 之前版本的 Go SDK | 存在日志轮转 Bug，长期运行会导致磁盘打满 | 升级 Go SDK 至 2512 及以上版本；临时处理：`> logfile` 截断日志 | 否 |
| **Python SDK RPM 包安装失败** | P2 (计划处理) | 安装报错 `cpio: File from package already exists as a directory` | Python SDK | 系统已有 `pytz` 目录，与 RPM 包冲突 | SDK 无法通过 RPM 正常安装 | 备份并移动冲突的 `pytz` 目录后重新安装 | 否 |
| **链路增加延迟，对时间敏感业务有影响** | P2 (评估处理) | 时间敏感服务延迟加大，网络出现延迟 | PCM SDK / 业务应用 | 接入 PCM 后，凭据获取/刷新增加网络链路耗时 | 影响时间敏感业务的 SLA | 升级 SDK，配置 `PCM_TASK_DELAY` 环境变量设置最大超时时间（默认 1000ms） | 否 |
| **PCM Core 服务异常或宕机** | P2 (计划处理) | PCM Core 存活监控/SDK 请求 Core 失败日志 | PCM Core / PCM SDK | 运行时 PCM Core 挂了，或网络不通/超时，且 SDK 本地缓存未丢失 | SDK 无法从 Core 获取新凭证，触发降级逻辑读取本地加密文件返回老凭证（未在窗口期末尾），业务无影响 | 排查 PCM Core 宕机或网络异常原因 | 否 |
| **PCM 初始化服务异常** | P2 (计划处理) | SDK 初始化日志/PCM Core 就绪状态监控 | PCM Core / PCM SDK | 新部署时 PCM Core 还未 ready，或产品独立升级时 PCM 未 ready | PCM 服务未就绪，SDK 触发容错降级，将入参（底表 AK）作为凭证返回，业务无影响 | 等待 PCM Core 就绪或排查启动异常 | 否 |
| **客户端产生大量 PCM 相关 WARN 日志** | P3 (暂缓处理) | 产品日志中大量 `Failed to refresh credential...` 等 WARN 日志 | 各语言 SDK | PCM 服务端未部署/不可达，或版本不匹配 | SDK 降级返回原始凭证，不影响业务，但触发客户端告警监控 | 部署 PCM 服务端，或调整客户端日志级别/告警阈值 | 否 |
| **部分 SDK 未打印关键日志，排查困难** | P3 (暂缓处理) | 排查问题时找不到 requestid 等关键信息 | Java SDK 等 | 产品侧屏蔽了过多的 Java WARN 日志 | 增加问题排查难度 | 调整日志配置，保留 PCM 相关的关键报错日志 | 否 |
| **派生AK白屏查询缺失** | P3 (暂缓处理) | 白屏未查询到特定AK | PCM | 派生队列超过14把AK或派生超过14天 | 白屏仅展示最近14把/14天内的派生AK，超出的在ummak侧删除但pcm库保留，导致白屏查不到 | 见下文【启用派生AK】中的数据库查询方案 | 否 |
| **平台 AK 访问日志不可用** | P3 (暂缓处理) | PCM Controller 日志/访问日志状态监控 | PCM Controller | 平台 AK 访问日志系统不可用或数据不可信 | PCM 无法确认即将禁用的派生 AK 是否仍有产品在调用，将在第一把队列即将禁用时停止轮转以保护凭证 | 排查平台 AK 访问日志系统故障 | 否 |
| **SDK 超时日志毫秒数为 null** | P4 (忽略) | 超时日志中毫秒数字段显示为 null | SDK | 未设置 `PCM_TASK_DELAY` 时默认 1s 超时 | 已知日志格式问题，不影响功能 | 无需处理，或升级 SDK 版本 | 否 |

## 应急手册

### 容量告警场景

> 注：家里测试环境出现过，现场暂未出现。
> 应急参考文档：[《容量问题数据处理》](https://alidocs.dingtalk.com/i/nodes/QG53mjyd800agdlKHbek2aXQ86zbX04v)

**触发条件**：UMMAK侧每个uid下最大1000把有效AK，当达到1000把以后会出现派生失败的情况。

**1. 查询排查**
*   检查特定uid下（如1000000047）的ak数量：
    ```sql
    SELECT user_id, COUNT(access_id) AS access_count FROM accesskey_table where user_id = '1000000047' GROUP BY user_id;
    ```
*   查询是否有uid下的ak超过1000：
    ```sql
    SELECT user_id, COUNT(access_id) AS access_count FROM accesskey_table GROUP BY user_id HAVING access_count >= 1000;
    ```

**2. 清理止血**
分析出环境内已经无用的ak，在ummak中置成删除状态：
```sql
update accesskey_table set enabled_flag = 0, deleted_flag = 1 , modified_time = UNIX_TIMESTAMP() where access_id in (xxxxx);
```

### 启用某个已经禁用的initAK

**适用场景**：确认因为某把AK被禁用而影响业务。

*   **白屏操作**：通过PCM控制台的 initAK 管理功能查询特定AK，并在操作中启用该AK。
    ![image.png](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/Lk3lbmbxZoXdJOm9/img/71c6499e-968c-4b82-aa25-e3093bc7d9b7.png)
*   **调用接口（容器中执行脚本）**：当白屏不可用时，采用此方案。参考：[《工具》](https://alidocs.dingtalk.com/i/nodes/7NkDwLng8Za7QYkeHxdzN0A7JKMEvZBY?utm_scene=team_space&iframeQuery=anchorId%3Duu_mocpgly2iwborsrkk7e)
*   **数据库中操作**：当白屏、容器均不可用时，采用此方案。
    1. AK状态存储在UMMAK数据库中，进入UMMAK数据库（service：`baseService-umm-ak`，db实例：`ummak`，数据库：`ummak`）。
    2. 执行SQL：
       ```sql
       update accesskey_table set enabled_flag=1 where access_id = {akid};
       ```

### 启用全量底表AK

**适用场景**：环境内存在被底表AK禁用而影响业务，涉及多把底表AK或无法确认某把底表AK，可采用启用全量底表AK。
**注意：暂不支持通过白屏解禁全量AK。**

*   **调用接口（容器中执行脚本）**：参考：[《工具》](https://alidocs.dingtalk.com/i/nodes/7NkDwLng8Za7QYkeHxdzN0A7JKMEvZBY?utm_scene=team_space&iframeQuery=anchorId%3Duu_mocpgly2iwborsrkk7e)
*   **数据库操作**：当容器不可访问时，采用此方案。
    1. **获取全量底表AK**：pcm托管的底表AK存储在 clm_db 实例的 pcm 数据库中（service：`certificate-lifecycle-manager-server`，db实例：`clm_db`，数据库：`pcm_db`）。
       * 进入clm_db实例数据库后切换到pcm_db：
         ```sql
         use pcm_db;
         ```
       * 检索已经禁用的initAK：
         ```sql
         select access_key_id from init_ak_info where umm_ak_status = 0;
         ```
    2. **启用全量底表AK**：进入UMMAK数据库（service：`baseService-umm-ak`，db实例：`ummak`，数据库：`ummak`）。
       * 执行SQL（执行前，将access_id字段参数改成步骤一中检索到的底表ak信息）：
         ```sql
         update accesskey_table set enabled_flag=1 where access_id in ('qNNm2yFXF70Zy6Hx','qNNm2yFXF70Zy6Hx2','qNNm2yFXF70Zy6Hx3');
         ```

### 启用派生AK

**使用场景**：确认某把派生AK被禁用影响业务。

*   **白屏操作**：白屏支持查询派生AK，查询后可通过启用操作恢复。
    ![image.png](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/Lk3lbmbxZoXdJOm9/img/610ebe22-d389-44fe-a31b-cce2b4c221c2.png)
*   **数据库操作**：
    > **注意事项**：每个派生队列中通过白屏仅可以查询最近14把派生AK，如果超过14把AK后，会在ummak侧执行删除操作，但pcm数据库会保留派生AK记录。当通过白屏未查询到该AK，有可能是14天前派生的ak，可通过pcm数据库进行查询。

    1. **查询派生AK**：进入 clm_db 实例的 pcm_db 数据库（service：`certificate-lifecycle-manager-server`，db实例：`clm_db`，数据库：`pcm_db`）。
       ```sql
       use pcm_db;
       ```
    2. **在UMMAK中启用**：进入UMMAK数据库（service：`baseService-umm-ak`，db实例：`ummak`，数据库：`ummak`）。
       * 如果存在，直接更新启用状态：
         ```sql
         update accesskey_table set enabled_flag=1,hidden_flag=0,deleted_flag=0 where access_id='qNNm2yFXF70Zy6Hx';
         ```
       * 如果已经删除，创建ak（说明：access_id为akid，access_key为sk，user_id为账号）：
         ```sql
         INSERT INTO `ummak`.`accesskey_table` (`access_id`, `access_key`, `user_id`) VALUES ('000cFXr3DBPZHxML11', 'XE5sP5dF6asjJsCkxL4QYifS7rRU11', '999999999');
         ```

## 补充说明

> 1. 对于下游云服务（如 OSS、SLS、ASAPI、KMS 等）因 AK 禁用导致的访问拦截告警，统一设定为 P1（立即止血）级别。具体处置时请提取请求 AK 查询状态，并严格参考《PCM应急处置》文档执行。
> 2. 原始资料中提及的 `OSS_OCM_INNER` 无具体日志特征内容，`ODPS` 日志示例中未体现明确的 AK 禁用拦截报错特征，故暂未列入本汇总表。后续若有明确特征可继续补充。
> 3. 告警级别说明：P1 对应高优先级（需立即止血），P2 对应中优先级（计划/评估处理），P3 对应低优先级（暂缓处理），P4 为可忽略项。