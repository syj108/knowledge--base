# 告警-风险-异常汇总表

针对 PCM 已知各类问题的应急处置方案及运维参考链接：

*   [《PCM应急处置》](https://alidocs.dingtalk.com/i/nodes/MNDoBb60VLYDGNPytBomBqkPJlemrZQ3)
*   [《PCM运维手册》](https://alidocs.dingtalk.com/i/nodes/amweZ92PV6DbOdgzUK4on0qD8xEKBD6p)
*   [《PCM排查思路&常见问题》](https://alidocs.dingtalk.com/i/nodes/m9bN7RYPWdyrPBREckdQ5joEVZd1wyK0)
*   [EOCC 预案：PCM Controller 磁盘打满/大量日志应急](https://eocc.aliyun-inc.com/kbscene/emergencyDetail/EC9EE9AE20?Jump=2)

## 异常识别与处置总则

当遇到访问报错，怀疑是 PCM 禁用 AK 导致的，优先通过拦截日志判定。提取日志中的请求 AK，并通过 PCM 服务查询 AK 状态；如果确认已经禁用，应立即采用应急处置方案进行止血，并反馈研发侧排查根本原因。

**应急处置优先级原则**：
应急操作优先建议控制台白屏操作，当白屏无法访问时，采用在容器中执行脚本（调用服务接口），当容器无法访问时，直接在数据库中执行 SQL。
**控制台白屏 > 调用接口（容器脚本） > 数据库执行 SQL**

## 告警/风险/异常汇总表

以下汇总表按优先级（P0 立即止血 -> P2 暂缓/忽略）排列，旨在帮助值班人员快速识别问题严重程度并处置各类异常与风险。

| 告警名 | 级别 | 识别方式 | 所属组件 | 触发条件 | 含义 | 应急手册链接 | 是否有EOCC/KB |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **AK 调用网关被拦截** | P0 (立即止血) | 网关日志报 AK 被禁用/无效/不存在；产品调用网关报错 | PCM Core / 网关 / SDK | 底表AK被禁用且SDK降级失败，或派生AK被轮转禁用且未及时更新 | 产品无法使用有效AK调用网关，业务请求被拦截 | 1. 在PCM控制台启用底表AK恢复业务<br>2. 重启服务刷新派生AK并停止该队列轮转<br>3. 参见[《PCM应急处置》](https://alidocs.dingtalk.com/i/nodes/MNDoBb60VLYDGNPytBomBqkPJlemrZQ3) | 否 |
| **下游云服务访问拦截 (OSS/SLS/ASAPI/KMS等)** | P0 (立即止血) | 日志包含 `InvalidAccessKeyId`/`403`/`401`/`Unauthorized`/`AccessKey is disabled` 等特征 | OSS / SLS / ASAPI / KMS | PCM禁用AK导致访问下游云服务报错 | AK被禁用，下游云服务拒绝访问 | 提取请求AK查询状态，若已禁用则采用 [《PCM应急处置》](https://alidocs.dingtalk.com/i/nodes/MNDoBb60VLYDGNPytBomBqkPJlemrZQ3) 并反馈研发排查 | 否 |
| **UMMAK 单 UID AK 容量超限** | P0 (立即止血) | 派生失败报错 / 数据库查询超限UID | UMMAK / PCM | 单个UID下有效AK数量达到1000把 | 达到上限后会导致新的AK派生失败 | [容量问题数据处理](https://alidocs.dingtalk.com/i/nodes/QG53mjyd800agdlKHbek2aXQ86zbX04v) / 见下文【容量告警场景】 | 否 |
| **UMM 账户 AK 上限打满 (多集群)** | P0 (立即止血) | UMM 账户 AK 数量监控/创建派生 AK 失败日志 | UMM / PCM Controller | 派生 AK 队列级别配置为 ClusterName 级别，且存在多集群 | 多集群叠加把 UMM 账户的 AK 上限打满，导致无法创建新派生 AK | 修改队列级别配置为 initAK 级别（默认推荐） | 否 |
| **initAK/底表AK/派生AK被禁用** | P0 (立即止血) | 业务报错 / 白屏或数据库查询AK状态 | PCM / UMMAK | 环境内存在被禁用的 initAK/底表AK/派生AK | 相关AK被禁用导致依赖该AK的业务请求或凭证派生失败 | 见下文【启用某个已经禁用的initAK】/【启用全量底表AK】/【启用派生AK】 | 否 |
| **底表禁用后服务重启无凭据** | P0 (立即止血) | 服务重启后无法获取凭据，业务直接中断 | 业务产品 / PCM 链路 | 底表AK被禁用，且此时PCM链路不可用，本地无缓存 | 服务重启时拿不到任何有效凭据，业务中断 | 确保PCM链路高可用；紧急情况下在PCM控制台临时启用底表AK | 否 |
| **PCM 与应用均异常且缓存丢失** | P0 (立即止血) | 应用启动失败/凭证获取失败告警 | PCM SDK / 云产品应用 | PCM 和应用都挂了需重拉，且 SDK 本地缓存丢失 | SDK 无法获取凭证且无本地缓存降级，导致业务中断 | 需先恢复 PCM 或使用老凭证应急脚本 | 否 |
| **PCM Controller 磁盘打满** | P0 (立即止血) | 磁盘空间不足告警；`df -h` 和 `du -sh` 检查日志目录异常 | PCM Controller | 大量异常请求或定时任务异常导致循环报错，且日志轮转配置不正常 | Controller日志目录出现超大文件，磁盘空间被耗尽 | 1. 清理历史日志<br>2. 排查异常请求/任务<br>3. 确认日志轮转配置<br>4. 参考 [EOCC预案](https://eocc.aliyun-inc.com/kbscene/emergencyDetail/EC9EE9AE20?Jump=2) | 是 |
| **Java 应用线程阻塞** | P0 (立即止血) | 线程 dump 中出现 `BLOCKED` 在 `NativePRNG$RandomIO` | Java SDK | SDK使用 `/dev/random` 阻塞模式，系统熵值低（<100）。存量版本 < 1.0.8 | 应用线程被卡住，导致业务阻塞 | 1. 升级SDK至 `credprovider.plugin >= 1.0.8`<br>2. 临时规避：JVM参数 `-Djava.security.egd=file:/dev/./urandom` | 否 |
| **Core 认证/参数失败 (403/400)** | P0 (立即止血) | Core 返回 403（signature error/nbf not valid）或 400（参数为空/AK非底表） | PCM Core / SDK | regionid/AK/SK/endpoint配置错误、SK未解密、时钟不同步 | 签名验证失败或参数校验失败，请求被拒绝 | 1. 排查配置及NTP同步<br>2. 确认SK已解密<br>3. 参考 Core 错误码快速定位 | 否 |
| **CLI 工具报错 ResponseParseFailure** | P0 (立即止血) | CLI 报错 `{"code": "ResponseParseFailure", ...}` | CLI 工具 | `pcm_endpoint` 地址错误，响应200但格式非预期，CLI解析失败且未走降级 | CLI 工具直接不可用 | 1. 确认 `pcm_endpoint` 指向正确地址并手动 curl 验证<br>2. 升级至 2025-12-23 及之后版本 | 否 |
| **Core 返回 502 (限流/误伤)** | P1 (尽快处理) | SDK 报错 HTTP 502；access.log 中 `limit_req_status` 字段异常 | PCM Core | QPS超阈值；或同IP下其他高频产品耗尽配额导致误伤 | 请求被 PCM Core 限流拦截 | 1. 检查 access.log 和使用 `tsar` 查看 QPS<br>2. 调整限流配置 `pcm_core.json` | 否 |
| **半轮转模式首次获取失败** | P1 (尽快处理) | 产品持续使用底表 AK 或无有效凭据运行，不会自动恢复 | 业务产品 / SDK | 采用半自动轮转模式，且首次获取失败（限流/网络抖动等） | 产品无法自动恢复获取派生AK，存在凭据失效风险 | 重启服务以重新触发获取，或改造为持续轮转模式 | 否 |
| **Go SDK 日志文件不轮转** | P1 (尽快处理) | Go SDK 产生的日志文件不断增大，未按预期轮转 | Go SDK | Go SDK 在 2512 之前版本存在日志轮转 Bug | 日志持续增大，存在磁盘打满风险 | 1. 升级 Go SDK 至 2512 及以上版本<br>2. 临时处理：`> logfile` 截断日志（勿 rm） | 否 |
| **PCM Core 服务异常或宕机** | P1 (尽快处理) | PCM Core 存活监控/SDK 请求 Core 失败日志 | PCM Core / PCM SDK | 运行时 PCM Core 挂了，或网络不通/超时，且 SDK 本地缓存未丢失 | SDK 触发降级逻辑读取本地加密文件返回老凭证，业务暂无影响 | 排查 PCM Core 宕机或网络异常原因 | 否 |
| **PCM 初始化服务异常** | P1 (尽快处理) | SDK 初始化日志/PCM Core 就绪状态监控 | PCM Core / PCM SDK | 新部署时 PCM Core 还未 ready，或产品独立升级时 PCM 未 ready | PCM 服务未就绪，SDK 触发容错降级返回入参底表 AK，业务暂无影响 | 等待 PCM Core 就绪或排查启动异常 | 否 |
| **客户端产生大量 PCM WARN 日志** | P2 (暂缓处理) | 产品日志中大量 `Failed to refresh credential...` | 各语言 SDK | PCM服务未部署/不可达，或版本不匹配。SDK持续尝试连接失败 | SDK降级返回原始凭证，不影响业务，但触发告警监控产生大量日志 | 升级服务端或 baseServiceAll；调整日志级别或忽略该告警 | 否 |
| **链路增加延迟影响时间敏感业务** | P2 (暂缓处理) | 时间敏感服务延迟加大 | SDK / 网络 | 接入 PCM 后网络或处理增加延迟 | 对时间敏感业务造成延迟影响 | 使用 1.13-SNAPSHOT (20250908) 及以上版本，设置 `PCM_TASK_DELAY` 环境变量控制最大超时时间（默认1000ms） | 否 |
| **Python SDK RPM 包安装失败** | P2 (暂缓处理) | 安装报错 `cpio: File from package already exists as a directory` | Python SDK | 系统已有 `pytz` 目录，与 RPM 包冲突 | RPM 包无法正常安装 | 备份并移动冲突目录 `mv .../pytz .../pytz_bak`，然后重新 `yum install` | 否 |
| **部分 SDK 未打印关键日志** | P2 (暂缓处理) | 排查问题时找不到 requestid 等关键信息 | Java SDK / 业务产品 | 产品侧屏蔽了过多的 Java WARN 日志 | 增加问题排查难度 | 建议产品侧放开关键日志或调整日志级别 | 否 |
| **派生AK白屏查询缺失** | P2 (暂缓处理) | 白屏未查询到特定AK | PCM | 派生队列超过14把AK或派生超过14天 | 白屏仅展示最近14把/14天内的派生AK，超出的在ummak侧删除但pcm库保留 | 见下文【启用派生AK】中的数据库查询方案 | 否 |
| **平台 AK 访问日志不可用** | P2 (暂缓处理) | PCM Controller 日志/访问日志状态监控 | PCM Controller | 平台 AK 访问日志系统不可用或数据不可信 | PCM 无法确认即将禁用的派生 AK 是否仍有调用，将停止轮转以保护凭证 | 排查平台 AK 访问日志系统故障 | 否 |
| **SDK 超时日志毫秒数为 null** | P2 (忽略) | 超时日志中毫秒数字段显示 null | SDK | 未设置 `PCM_TASK_DELAY` 时默认 1s 超时 | 已知日志格式问题，不影响功能 | 无需处理，或升级 SDK 版本 | 否 |

## 应急手册

### 容量告警场景

> 注：家里测试环境出现过，现场暂未出现。
> 应急参考文档：[《容量问题数据处理》](https://alidocs.dingtalk.com/i/nodes/QG53mjyd800agdlKHbek2aXQ86zbX04v)

**触发条件**：UMMAK侧每个uid下最大1000把有效AK，当达到1000把以后会出现派生失败的情况。

**1. 查询排查**
*   检查特定uid下（如1000000047）的ak数量：
    ```sql
    SELECT user_id, COUNT(access_id) AS access_count FROM accesskey_table where user_id = '1000000047' GROUP BY user_id;
    ```
*   查询是否有uid下的ak超过1000：
    ```sql
    SELECT user_id, COUNT(access_id) AS access_count FROM accesskey_table GROUP BY user_id HAVING access_count >= 1000;
    ```

**2. 清理止血**
分析出环境内已经无用的ak，在ummak中置成删除状态：
```sql
update accesskey_table set enabled_flag = 0, deleted_flag = 1 , modified_time = UNIX_TIMESTAMP() where access_id in (xxxxx);
```

### 启用某个已经禁用的initAK

**适用场景**：确认因为某把AK被禁用而影响业务。

*   **白屏操作**：通过PCM控制台的 initAK 管理功能查询特定AK，并在操作中启用该AK。
    ![image.png](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/Lk3lbmbxZoXdJOm9/img/71c6499e-968c-4b82-aa25-e3093bc7d9b7.png)
*   **调用接口（容器中执行脚本）**：当白屏不可用时，采用此方案。参考：[《工具》](https://alidocs.dingtalk.com/i/nodes/7NkDwLng8Za7QYkeHxdzN0A7JKMEvZBY?utm_scene=team_space&iframeQuery=anchorId%3Duu_mocpgly2iwborsrkk7e)
*   **数据库中操作**：当白屏、容器均不可用时，采用此方案。
    1. AK状态存储在UMMAK数据库中，进入UMMAK数据库（service：`baseService-umm-ak`，db实例：`ummak`，数据库：`ummak`）。
    2. 执行SQL：
       ```sql
       update accesskey_table set enabled_flag=1 where access_id = {akid};
       ```

### 启用全量底表AK

**适用场景**：环境内存在被底表AK禁用而影响业务，涉及多把底表AK或无法确认某把底表AK，可采用启用全量底表AK。
**注意：暂不支持通过白屏解禁全量AK。**

*   **调用接口（容器中执行脚本）**：参考：[《工具》](https://alidocs.dingtalk.com/i/nodes/7NkDwLng8Za7QYkeHxdzN0A7JKMEvZBY?utm_scene=team_space&iframeQuery=anchorId%3Duu_mocpgly2iwborsrkk7e)
*   **数据库操作**：当容器不可访问时，采用此方案。
    1. **获取全量底表AK**：pcm托管的底表AK存储在 clm_db 实例的 pcm 数据库中（service：`certificate-lifecycle-manager-server`，db实例：`clm_db`，数据库：`pcm_db`）。
       * 进入clm_db实例数据库后切换到pcm_db：
         ```sql
         use pcm_db;
         ```
       * 检索已经禁用的initAK：
         ```sql
         select access_key_id from init_ak_info where umm_ak_status = 0;
         ```
    2. **启用全量底表AK**：进入UMMAK数据库（service：`baseService-umm-ak`，db实例：`ummak`，数据库：`ummak`）。
       * 执行SQL（执行前，将access_id字段参数改成步骤一中检索到的底表ak信息）：
         ```sql
         update accesskey_table set enabled_flag=1 where access_id in ('qNNm2yFXF70Zy6Hx','qNNm2yFXF70Zy6Hx2','qNNm2yFXF70Zy6Hx3');
         ```

### 启用派生AK

**使用场景**：确认某把派生AK被禁用影响业务。

*   **白屏操作**：白屏支持查询派生AK，查询后可通过启用操作恢复。
    ![image.png](https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/Lk3lbmbxZoXdJOm9/img/610ebe22-d389-44fe-a31b-cce2b4c221c2.png)
*   **数据库操作**：
    > **注意事项**：每个派生队列中通过白屏仅可以查询最近14把派生AK，如果超过14把AK后，会在ummak侧执行删除操作，但pcm数据库会保留派生AK记录。当通过白屏未查询到该AK，有可能是14天前派生的ak，可通过pcm数据库进行查询。

    1. **查询派生AK**：进入 clm_db 实例的 pcm_db 数据库（service：`certificate-lifecycle-manager-server`，db实例：`clm_db`，数据库：`pcm_db`）。
       ```sql
       use pcm_db;
       ```
    2. **在UMMAK中启用**：进入UMMAK数据库（service：`baseService-umm-ak`，db实例：`ummak`，数据库：`ummak`）。
       * 如果存在，直接更新启用状态：
         ```sql
         update accesskey_table set enabled_flag=1,hidden_flag=0,deleted_flag=0 where access_id='qNNm2yFXF70Zy6Hx';
         ```
       * 如果已经删除，创建ak（说明：access_id为akid，access_key为sk，user_id为账号）：
         ```sql
         INSERT INTO `ummak`.`accesskey_table` (`access_id`, `access_key`, `user_id`) VALUES ('000cFXr3DBPZHxML11', 'XE5sP5dF6asjJsCkxL4QYifS7rRU11', '999999999');
         ```

## 补充说明

> 1. 对于下游云服务（如 OSS、SLS、ASAPI、KMS 等）因 AK 禁用导致的访问拦截告警，统一设定为 P0（立即止血）级别。具体处置时请提取请求 AK 查询状态，并严格参考《PCM应急处置》文档执行。
> 2. 原始资料中提及的 `OSS_OCM_INNER` 无具体日志特征内容，`ODPS` 日志示例中未体现明确的 AK 禁用拦截报错特征，故暂未列入本汇总表。后续若有明确特征可继续补充。
> 3. 告警级别说明：P0 对应最高优先级（需立即止血以恢复业务），P1 对应高优先级（需尽快处理以消除隐患），P2 对应中低优先级（可暂缓处理、评估或忽略）。